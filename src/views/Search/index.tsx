// components

const SearchView = ({ pageData }: ANY_OBJECT) => {
    console.log("pageData__", pageData);
    return (
        <>
          <ul>
            <li></li>
          </ul>

        </>
    );
};

export default SearchView;
